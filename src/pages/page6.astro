---
import { getEntry, render } from 'astro:content';
import PageLayout from '../layouts/PageLayout.astro';

const entry = await getEntry('pages', 'page-6');
if (!entry) throw new Error('Missing content entry: page-6');
const { Content } = await render(entry);
const { title, subtitle, layout, backgroundImage, heroImage, imageAlt, youtubeId, externalLink, externalLinks, order } = entry.data;
---

<PageLayout
  title={title}
  subtitle={subtitle}
  layout={layout}
  backgroundImage={backgroundImage}
  heroImage={heroImage}
  imageAlt={imageAlt}
  youtubeId={youtubeId}
  externalLink={externalLink}
  externalLinks={externalLinks}
  order={order}
>
  <Content />
</PageLayout>
